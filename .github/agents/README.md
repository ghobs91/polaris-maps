# Custom Agents

This directory contains workspace-scoped custom Copilot agents.

Available agents:

- `Debug Repair`
- `Product Engineer`

If they do not appear immediately in the agent picker, run `Developer: Reload Window` in VS Code.
